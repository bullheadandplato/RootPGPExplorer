<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cryptopaths.cryptofm">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/iconfinal"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".startup.InitActivity"
            android:configChanges="keyboardHidden|orientation|screenSize" />
        <activity
            android:name=".startup.UnlockDbActivity"
            android:configChanges="keyboardHidden|orientation|screenSize" />
        <activity
            android:name=".filemanager.FileBrowserActivity"
            android:configChanges="keyboardHidden|orientation|screenSize" />

        <!-- first and splash activity -->
        <activity
            android:name=".startup.SplashScreen"
            android:label="@string/app_name"
            android:theme="@style/SplashTheme" />

        <!-- Pre start activity -->
        <activity
            android:name=".filemanager.FilemanagerTabs"
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:theme="@style/PreActivityThemeFragmentOne">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".startup.OptionActivity" />
        <activity android:name=".startup.KeySelectActivity" />
        <!-- End of activities -->
        <service
            android:name=".services.CleanupService"
            android:stopWithTask="false" />

        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>

    </application>

</manifest>